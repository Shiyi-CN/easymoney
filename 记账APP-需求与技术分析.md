# 安卓记账 APP — 需求与技术分析

> 生成日期：2026-05-17
> 目标平台：Android
> 开发方式：原生 Android（Kotlin / Jetpack Compose）

---

## 一、核心功能模块

### 1. 日常收支记录
- **快速记账**：首页一键新建记账，支持金额、分类、日期、备注
- **双模式**：收入 / 支出，默认支出
- **必填项**：金额（数字键盘）、分类（选择器）
- **选填项**：备注、日期时间（默认当前）、图片凭证
- **记录列表**：按日期分组，时间倒序，每条显示：图标 + 分类 + 金额 + 备注摘要
- **编辑/删除**：左滑删除，点击进入编辑

### 2. 分类管理（支持自定义标签）
- **预设分类**：支出（餐饮、交通、购物、娱乐、居住、医疗、教育、其他）
  收入（工资、奖金、理财、兼职、红包、其他）
- **自定义标签**：用户可新增、编辑、删除自定义分类
- **分类图标**：每个分类关联 Material 图标，预设带图标，自定义可选图标
- **多级分类**（可选）：如「餐饮 → 早餐/午餐/晚餐」，简化版可暂不支持

### 3. 月度/年度统计报表
- **月度概览**：当月总支出、总收入、结余，环形图（支出分类占比）
- **年度趋势**：12个月折线图/柱状图，展示每月收支趋势
- **分类明细**：点击月度环形图任一分区，下钻查看该分类当月明细记录
- **排行榜**：当月支出分类金额排序 Top5
- **同比/环比**：与上月/去年同月对比（百分比变化）

### 4. 数据导出
- **导出格式**：CSV（最通用，Excel/WPS 可直接打开）
- **导出范围**：全部数据 / 指定月份
- **导出路径**：导出到设备「下载 /Documents/ 记账App」目录
- **导出内容**：日期、分类、金额（元）、收支类型、备注

### 5. 数据本地存储
- **全量本地化**：所有数据存储在设备本地，不上传任何服务器
- **自动备份**：每次记账自动保存，无需手动操作
- **备份还原**：支持导出数据库文件到外部存储，支持从备份恢复

---

## 二、技术需求

### 1. UI/UX 设计规范
| 维度 | 要求 |
|------|------|
| **设计语言** | Material Design 3 (Material You)，适配 Android 12+ 动态取色 |
| **最低 SDK** | Android 7.0 (API 24) |
| **目标 SDK** | Android 14 (API 34) |
| **布局规范** | 单栏布局（手机）、手势导航、底部导航栏（首页/统计/设置） |
| **适配** | 横竖屏锁定为竖屏（记账场景单手操作），适配刘海屏/挖孔屏 |
| **暗黑模式** | 支持跟随系统主题自动切换浅色/深色模式 |
| **无障碍** | 文本按钮足够大（≥48dp），支持 TalkBack 内容描述 |

### 2. 本地数据库方案

**推荐方案：Room（Android 官方 ORM 层，基于 SQLite）**

| 要素 | 说明 |
|------|------|
| **ORM** | Room 数据库（Jetpack 官方组件） |
| **SQLite 版本** | 通过 Room 版本管理自动升级 |
| **核心表结构** | `records`（收支记录）、`categories`（分类）、`attachments`（附件） |
| **数据访问** | DAO 模式 + Flow（响应式查询，自动通知 UI 更新） |
| **事务** | 批量导入/导出使用 `@Transaction` 保证原子性 |

**表结构设计（精简）：**

```
records
  id          INTEGER PRIMARY KEY AUTOINCREMENT
  type        INTEGER NOT NULL  -- 0=支出, 1=收入
  amount      REAL NOT NULL
  category_id INTEGER REFERENCES categories(id)
  note        TEXT
  date        INTEGER NOT NULL  -- Unix 时间戳(毫秒)
  created_at  INTEGER NOT NULL

categories
  id          INTEGER PRIMARY KEY AUTOINCREMENT
  name        TEXT NOT NULL UNIQUE
  icon        TEXT  -- Material Icon 名称
  type        INTEGER NOT NULL  -- 0=支出, 1=收入
  is_preset   INTEGER NOT NULL DEFAULT 1  -- 1=预设, 0=自定义
  sort_order  INTEGER
```

**备选方案对比（不建议）：**

| 方案 | 劣势 |
|------|------|
| SQLite 裸用 | 代码量大、无类型安全、需手动管理版本迁移 |
| Realm | 库体积大、Kotlin 协程支持差、已进入维护模式 |
| DataStore | 仅适合 KV 配置，不适合结构化记录数据 |

### 3. 数据安全与隐私策略

| 风险 | 应对措施 |
|------|---------|
| **设备丢失** | 无默认保护 → 引导用户开启设备锁屏密码，App 内支持应用锁（手势/指纹） |
| **数据泄露** | 数据仅存本地，不联网 → 无需网络权限（`INTERNET` 权限不声明） |
| **备份暴露** | 导出 CSV 不含敏感字段；数据库备份文件加密（AES-256-GCM，密钥派生自用户手势密码） |
| **Android 权限** | 最小权限原则：仅申请 `WRITE_EXTERNAL_STORAGE`（导出时，Android 10+ 用 SAF） |
| **日志安全** | 生产包关闭 `Log.d`；禁止在日志中输出用户输入的完整备注内容 |
| **隐私合规** | 不收集任何设备标识（IMEI/Android ID）；隐私政策声明"零数据离设备" |

---

## 三、技术栈推荐总结

```
├── 语言         Kotlin
├── UI 框架      Jetpack Compose + Material3
├── 数据库       Room + SQLite
├── 图表库       MPAndroidChart（成熟）或 Vico Compose（原生 Compose 图表）
├── 导航         Navigation Compose
├── 依赖注入     Hilt
├── 架构模式     MVVM（ViewModel + Repository + Room）
├── 异步         Kotlin Coroutines + Flow
└── 构建工具     Gradle KTS + AGP
```

---

## 四、进一步建议

### 1. 开发路线（MVP → 迭代）
- **MVP（第 1 版）**：记账列表 + 预设分类 + 月度环形图 + CSV 导出
- **V2**：自定义分类 + 年度趋势 + 搜索
- **V3**：应用锁 + 数据备份还原 + 多币种
- **V4**：预算管理 + 账单提醒 + iCloud/Google Drive 同步（可选）

### 2. 建议避免的方向（保持简单）
- **不要一开始做云同步**：增加服务端成本、隐私合规复杂
- **不要做社交功能**：记账是私密行为，加社交偏离核心价值
- **不要做 AI 自动分类**：用户自定义分类比自动识别更准确，且省去模型部署成本
- **不要做 OCR 识别发票**：投入产出比低，MVP 阶段不适用

### 3. 可选加分功能（后期考虑）
- **应用小组件（Widget）**：桌面快捷记账入口
- **多账本**：区分个人/家庭/工作账本
- **预算提醒**：设定月度预算，超支通知
- **i18n 国际化**：支持中/英切换

### 4. 测试策略
- **单元测试**：Repository + ViewModel 逻辑，使用 Room 内存数据库
- **UI 测试**：Compose 测试框架 + 截图对比
- **数据完整性测试**：模拟异常退出验证事务原子性

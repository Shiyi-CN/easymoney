# 「记一下」极简记账设计思路

> 核心理念：**支付完成 = 记账完成，零额外操作**

---

## 零、命名哲学

应用名「记一下」已经传递了核心定位：
- **"一下"** — 不是"记半小时"，不是"认真盘点"，而是随手一笔
- 用户心理预期：耗时 < 3 秒

---

## 一、核心设计原则

```
1. 能一步到位的，绝不两步
2. 能自动识别的，绝不手动输入
3. 能默认选好的，绝不让人选择
4. 能事后补充的，不让记账时刻纠结
```

---

## 二、支付即记账的三大路径

按难度从低到高排列，建议 MVP 直接做路径一，路径二/三后续迭代。

### 路径一：桌面快捷入口（MVP 实现）⭐

**用户场景：** 刚付完钱，掏出手机 → 桌面点一下 → 完成

| 入口 | 实现方案 |
|------|---------|
| **主屏幕 Widget（重点）** | 2×2 组件：数字键盘 + 常用分类图标 4 个 + 确认按钮。无需打开 App，直接在桌面操作 |
| **App Shortcut** | 长按图标快捷菜单：「记一笔餐饮」「记一笔交通」「记一笔购物」— 点完直接弹出金额输入框，分类已锁定 |
| **快捷设置磁贴** | 下拉通知栏，点「快记」磁贴 → 展开面板，金额 + 分类快速选择 |
| **通知栏常驻** | 记一笔快捷通知，常驻状态栏，点开直接快速记录 |

**设计细节（Widget）：**
```
 ┌─────────────────────┐
 │   ¥ [  38.50  ]     │
 │                     │
 │ [🍔][🚕][🛒][🎮]     │
 │                     │
 │      [✓ 记一下]      │
 └─────────────────────┘
```
- 打开手机 → 右滑到 Widget 页 → 输入金额 → 按分类（预设上次用的）→ 完成
- 默认选中「上次使用的分类」，90% 场景只需点一下分类、按确认
- 最短路径：**2 次点击 = 完成记账**

---

### 路径二：短信/通知智能识别（v2 实现）

**原理：** 用户授权「通知读取权限」，App 在本地解析支付类通知，提取金额和商户名。

**支持的支付渠道：**
- 银行扣款短信：「尾号1234支出38.50元，商户：美团外卖」
- 支付宝/微信支付通知：「支付宝支出38.00元—全家便利店」
- 信用卡账单短信

**工作流程：**
```
收到支付通知 ─→ 本地解析金额/商户 ─→ 推一条通知：                  
                  "「记一下」检测到一笔支付：¥38.50 美团外卖
                   ← 忽略    记一笔 →"
                  用户点「记一笔」→ 自动填入金额、分类（餐饮），完成
```

**关键约束：**
- ❗ 所有解析在设备本地完成，不上传任何数据
- ❗ 通知读取需用户主动在系统设置中授权（跳转 `NotificationListenerService` 设置页）
- ❗ 可在首次弹出解释：「数据仅保留在本地，用于记账识别」
- ❗ 用户可随时关闭权限

**技术实现（NotificationListenerService）：**
- 核心组件：`NotificationListenerService`，这是 Android 系统的**事件驱动**服务，不是常驻轮询
- 工作方式：系统在收到新通知时**唤醒**该服务，处理完即**休眠**，空闲时不占用 CPU 和内存
- 与"后台保活"的区别：不持有 Wakelock，不自启，不轮询——**系统唤它才醒，干完活就睡**
- Android 8+ 自动管理其生命周期，被杀死后系统会自动重启
- 服务体积极小：仅 ~50KB，一个类 + 解析逻辑即可
- 用户打开 App 时 → 服务绑定到 App 进程（不新开进程）
- 用户切走 App → 服务转为系统托管（不消耗 App 内存）

**分类匹配规则（本地规则引擎，无 AI）：**
- 商户名中包含「外卖/餐厅/火锅/奶茶/咖啡」 → 分类: 餐饮
- 商户名中包含「打车/地铁/加油/停车/公交」 → 分类: 交通
- 商户名中包含「超市/便利店/百货」 → 分类: 购物

---

### 路径三：NFC/扫码支付联动（v3 探索）

- **NFC 标签桌贴**：在家里的书桌贴一个 NFC 标签，手机碰一下 → 预置分类「居家」+ 金额输入
- **NFC 场景**：办公桌贴标签 → 碰一下记一笔「咖啡/文具」
- **扫码**：购物小票二维码（部分超市收银小票含总金额条码）

---

## 三、"无痛记账"的 UX 细节

### 默认值策略（减少用户决策）
| 字段 | 默认值策略 |
|------|-----------|
| 分类 | 上一次使用的分类（用户习惯稳定） |
| 日期 | 当前时间（95% 场景正确） |
| 金额 | 空，必须输入（无法默认） |
| 备注 | 空，可选 |

### 语音记账（简化版）
- 点击输入框旁的 🎤 按钮
- 说 "午餐 38" → 自动填入金额 38、分类餐饮
- 说 "打车 25 块 5" → 自动填入金额 25.50、分类交通
- 使用 Android 本地语音识别（`SpeechRecognizer`），不联网

### 惯性记账（Pattern Learning）
- 如果用户在近 30 天里，工作日的 12:00-13:00 之间，有 ≥ 10 次 35-50 元的餐饮支出
- 那么在 12:30 推送提示：「午饭时间到了，记一笔？[¥45 | 餐饮] → 确认 / 修改」
- 这种模式匹配 100% 在设备本地计算，无需云端

### "随手拍"兜底
- 对于更懒的场景：直接拍照小票/收据，App 存为一条"待确认"记录
- 用户在有空时打开 App，补充金额和分类
- 关键：降低"必须现在记完整"的认知负担

### 静默模式
- 打开 App → 什么文字都不显示，直接弹出输入键盘 + 分类选择
- 记完自动回到桌面（如果启用了"记完即退"选项）
- 核心逻辑：**越少文字、越少按钮、越快**

---

## 四、Widget 的详细交互设计

Widget 是「记一下」的旗舰特性，值得重点打磨：

| 特性 | 实现 |
|------|------|
| 大小 | 2×2（最小可行）、4×2（带更多分类） |
| 内容 | 金额输入框 + 分类快捷图标 + 确认按钮 |
| 分类 | 显示最近使用的 4 个分类，长按可自定义排列 |
| 键盘 | 弹出数字键盘时自动隐藏分类行，留空间给数字 |
| 模式切换 | 左上角小按钮切换「支出/收入」 |
| 动画 | 确认后，显示 √ 动画 0.5 秒然后复位 |
| 状态保持 | 被系统清理后重新打开，恢复上次的分类选中状态 |

---

## 五、阶段性迭代路线

```
MVP（1-2周）               v2（1周）                  v3（2周）
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│ • 首页 Widget    │    │ • 通知智能识别  │    │ • 惯性记账建议  │
│ • 快速记账入口   │    │ • 语音记账      │    │ • NFC 标签支持  │
│ • 基础记账功能   │    │ • 批量导入      │    │ • 随手拍兜底    │
│ • 月度环形图     │    │ • 年度趋势图    │    │ • 小票识别      │
│ • CSV 导出       │    │ • 应用锁        │    │ • 预算提醒      │
│ • 预设分类       │    │ • 自定义分类    │    │ • 多账本        │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                           层级优先级：省力 > 好看 > 功能多
```

---

## 六、补充建议

### 优先做的（省力第一）
1. ✅ **Widget + App Shortcut** — 这是最省力的入口，优先级最高
2. ✅ **默认值智能推导** — 记住上次分类、高频时间段、高频金额
3. ✅ **数字键盘优化** — 支持"38.5"、"38.5"、"38" 三种输入，自动补零
4. ✅ **记完即退模式** — 记账完成后自动回到桌面，不让人多看一眼

### 坚决不做的（MBP 阶段）
- ❌ **做不完的账本** — 不要让人产生"记录压力"
- ❌ **社交分享** — 记账是最私密的事
- ❌ **复杂的预算规则** — 省力记账 ≠ 理财
- ❌ **AI 自动分类** — 简单的关键词匹配准确率已经 > 90%
- ❌ **多币种/汇率换算** — 除非目标用户明确需要

### 几个"离经叛道"但值得尝试的想法

**1. 负向激励 vs 正向激励**
- 大多数人记账坚持不了，因为「忘了记 → 算了不补了 → 破窗效应」
- 可尝试：每个周末弹出「本周您有 X 笔支付已自动识别，未确认的将自动归档」，降低罪恶感

**2. "记对优先，改在后头"**
- 默认行动是"记"，不是"编辑"——但前提是这条记录**大概率正确**
- 自动识别类记录（通知解析/语音/惯性建议）必须附带**置信度标记**：
  - ✅ 高置信度（金额精确匹配 + 商户关键词清晰）→ 直接记入，静默处理
  - ⚠️ 中置信度（金额匹配但商户名模糊）→ 记入但标记为"待确认"
  - ❓ 低置信度（金额模糊或无法识别商户）→ 不自动记，弹出确认询问

**待确认通知栏机制：**
- 创建一条**持久化通知**（ongoing notification），标题：「记一下 — X 笔记录待确认」
- 通知正文列出待确认记录摘要：每条一行「¥38.50 美团外卖 | ⚠️」显示金额和来源
- 点击通知 → 打开 App 直接跳转到「待确认列表」页面
- 用户在 App 内确认/修改一条记录后：
  - 实时更新通知内容（去掉已确认的那条）
  - 当待确认数量归零 → **自动清除该通知**
- 被标记为"待确认"的记录在 App 内以**黄色角标**显示，用户随时可点开确认或修改
- 如果当周有未确认的记录，周日周报时会再次提醒：「本周有 3 笔记录待确认，平均 ¥42」

**设置项：「待确认通知」**
- 位置：设置 → 通知设置 → 待确认通知开关
- 默认：**开启**
- 关闭后：不再显示待确认通知栏，但 App 内的黄色角标仍保留，周报提醒也不受影响
- 开关文案：「待确认通知」→ 副标题：「有新记录需要确认时在通知栏提醒」

- 核心逻辑：**记录要准，但纠错可以不急。不催你当场改，但不让你忘掉**

**3. 周报代替日报**
- 不要让人每天对账，太累
- 周报足以：每周日推送「本周花了 ¥2,350，比上周少 ¥300，👍」
- 只有周报没有日报，降低用户焦虑

**4. 按 App 自动分类支付**
- Android 可以获取前台 App 包名
- 检测到用户在美团/饿了么 → 打开记账 App 时自动选「餐饮」
- 检测到用户在滴滴/高德 → 自动选「交通」
- 完全在本地，不涉及个人数据上传

---

## 七、轻量化策略（APK < 5MB，内存 < 50MB）

### APK 体积控制

| 做法 | 效果 |
|------|------|
| 用 Android App Bundle (AAB) 分发 | Play Store 按需下发，用户实际下载 ≈ 2-3MB |
| 开启 R8 全量混淆压缩 | 减少 30%-50% DEX 体积 |
| **拒绝大库：不用 Hilt，不用 MPAndroidChart** | 省掉 ~2MB |
| 替代方案：手动 DI（简单场景用 `object` 单例），图表用自制 Canvas 绘制（月度环形图 + 年度柱状图，原生 Compose Canvas 足以） | |
| 矢量图标（Material Icons 按需引用），不用 PNG 切图 | 省掉 ~1MB |
| 资源瘦身：`resConfigs "zh"` 只打包中文资源 | 省掉 ~500KB |

**目标：APK 实际下载大小 ≤ 3MB**

### 内存占用控制

| 做法 | 说明 |
|------|------|
| 数据分页加载 | Room + Paging 3，不一次性加载全部记录到内存 |
| 图表按需渲染 | 只有图表所在 Tab 可见时才创建 Canvas，切走即释放 |
| 列表用 LazyColumn | 只渲染屏幕可见行，长列表不卡顿 |
| 图片附件缩略图 | 拍照记账的图片压缩至 480px 宽，用 Coil 按需加载 |
| 通知监听服务（NotificationListenerService） | **事件驱动**，无通知时零开销，系统托管生命周期，不自启不轮询。体积极小 ~50KB |
| 通知只存元数据 | 待确认通知内容仅存 ID 列表，金额/商户名从 Room 按需查询 |
| Flow 自动管理 | ViewModel 销毁时自动取消协程，无泄漏 |

### 一句话总结
> 这不是功能堆砌的工具，而是一个**轻量级的记账习惯伴侣**。小到不会让你犹豫要不要下载，快到打开就记、记完就走。

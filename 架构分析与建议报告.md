# 记一下 (JYX) 架构分析与建议报告

> 文档版本：v1.1（新增通知监听 Fix 专案）  
> 分析基准：APP 说明文档 v1.2.1（2026-06-08）  
> 报告日期：2026-06-09  
> 适用人员：开发者、技术协作方、产品评审

---

## 目录

1. [项目概览](#一项目概览)
2. [现状评估](#二现状评估)
3. [关键风险点](#三关键风险点)
4. [目标架构设计](#四目标架构设计)
5. [核心架构决策（ADR）](#五核心架构决策adr)
6. [版本路线图重新规划](#六版本路线图重新规划)
7. [目录结构优化方案](#七目录结构优化方案)
8. [优先行动清单](#八优先行动清单)
9. [通知监听 Fix 专案（小米 HyperOS）](#九通知监听-fix-专案小米-hyperos--miui)

---

## 一、项目概览

**记一下（JYX）** 是一款面向年轻人的极简记账 Android APP，主打"一秒记账"理念。

### 核心竞争力（差异化功能）

| 功能 | 差异化说明 |
|------|------------|
| 悬浮气泡记账 | 不需要打开 APP，屏幕边缘常驻入口 |
| 智能文本识别 | 输入"川南分公司打车50报销"自动解析金额、分类、报销对象 |
| 通知自动记账 | 监听微信/支付宝支付通知，自动提取账单 |
| 桌面宠物系统 | 将记账行为游戏化，提升用户长期留存（规划中） |

### 技术栈

| 技术 | 版本/说明 |
|------|-----------|
| 语言 | Kotlin |
| UI 框架 | Jetpack Compose |
| 数据库 | Room |
| 架构模式 | MVVM |
| 最低支持 | Android 7.0（API 24） |
| 目标版本 | Android 14（API 34） |

---

## 二、现状评估

### 做得好的地方

- **技术选型正确**：Kotlin + Compose + Room + MVVM 是当前 Android 开发最稳定的主线组合，官方支持好，社区资源丰富
- **功能差异化清晰**：悬浮气泡 + 智能识别 + 通知监听三者组合，形成了真实的竞争壁垒，不是简单的记账 CRUD
- **数据安全意识到位**：本地存储优先、备份恢复、CSV 导出，符合个人财务数据的隐私诉求
- **版本迭代节奏健康**：从 v1.0.0 到 v1.2.1 仅三周，功能迭代快，版本规划有逻辑

### 需要改进的地方

整体而言，当前架构是**典型的早期 MVVM 项目**，结构合理但存在几个随着功能增加会暴露的问题：

1. 缺少**领域层（UseCase）**，业务逻辑目前集中在 ViewModel 和工具类里
2. **智能识别逻辑**是核心竞争力，但没有被独立封装，难以复用和测试
3. **宠物系统**规划中，但尚未定义与记账核心的解耦方式
4. 存在一处**数据正确性 bug**（金额使用 Double 类型）

---

## 三、关键风险点

以下按风险优先级排序，**高风险需在下一个版本前解决**。

### 🔴 高风险

#### R-001：金额字段使用 `Double` 类型存储

**问题描述：**  
当前 `Record.amount` 字段为 `Double` 类型。浮点数在计算机中无法精确表示十进制小数，例如：

```
0.1 + 0.2 = 0.30000000000000004（而非 0.3）
```

在记账场景下，这会导致金额显示异常（¥38.10 显示为 ¥38.099999...）、统计汇总时误差累积。

**影响范围：** 数据库所有记录、月度/年度统计、CSV 导出  
**建议解决方案：** 参见 [ADR-001](#adr-001金额字段使用-long-存储单位分)

---

### 🟡 中风险

#### R-002：智能识别逻辑分散，缺乏统一封装

**问题描述：**  
v1.2.1 更新日志中出现"智能识别统一：磁贴和 APP 使用同一套逻辑"，说明在此之前两个入口逻辑不一致。当前的 `VoiceCategorizer.kt` 是工具类形式，同样的解析逻辑可能在通知监听、悬浮球、APP 内输入三处各有实现。

**影响范围：** 每次修改识别规则需要改多处；无法写单元测试；新增入口容易再次不一致  
**建议解决方案：** 参见 [ADR-002](#adr-002引入-usecase-层封装核心业务逻辑)

#### R-003：宠物系统与记账核心缺乏解耦设计

**问题描述：**  
宠物系统需要感知"用户记了一笔账"这个行为，最直觉的做法是在 `RecordViewModel` 里直接调用宠物相关逻辑。这会导致记账模块依赖宠物模块，形成不应有的耦合。

**影响范围：** 未来难以独立维护宠物逻辑；记账功能的测试会被宠物逻辑干扰  
**建议解决方案：** 参见 [ADR-003](#adr-003宠物系统通过领域事件与记账解耦)

---

### 🟢 低风险

#### R-004：`INTERNET` 权限被声明但无实际用途

**问题描述：**  
`AndroidManifest.xml` 中声明了 `android.permission.INTERNET`，但 APP 当前无任何网络请求功能。

**影响范围：** 应用商店（Google Play、国内应用市场）审核时可能被标记为权限过度申请  
**建议修复：** 如无近期网络功能计划，立即删除此权限声明

---

## 四、目标架构设计

### 4.1 整体分层结构

推荐在现有 MVVM 基础上，引入**领域层（Domain Layer）**，形成标准的**Clean Architecture 四层结构**：

```
┌─────────────────────────────────────────────────────────┐
│                    UI 层（Jetpack Compose）               │
│   主页/记账  │  统计/图表  │  报销管理  │  宠物/设置  │  悬浮球  │
└─────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────┐
│                   ViewModel 层（状态管理）                 │
│  RecordVM  │  StatisticsVM  │  ReimbursementVM  │  PetVM │
└─────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────┐
│              领域层 / UseCase（新增，建议引入）             │
│  SmartParseUseCase  │  AutoConfirmUseCase               │
│  PetBehaviorUseCase │  StatisticsUseCase                │
└─────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────┐
│                   Repository 层（数据门面）                │
│    RecordRepository  │  CategoryRepository  │  PetRepo  │
└─────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────┐
│                       数据层                             │
│  Room DB（DAO+Entity）  │  DataStore  │  Foreground Service │
└─────────────────────────────────────────────────────────┘
```

**依赖方向原则：** 上层依赖下层，下层不知道上层的存在。UI 不直接操作 Room，Service 不持有 ViewModel。

---

### 4.2 智能识别系统重构方案

将核心解析逻辑提取为独立的 UseCase：

```kotlin
// domain/usecase/SmartParseUseCase.kt
class SmartParseUseCase @Inject constructor(
    private val categoryRepo: CategoryRepository
) {
    data class ParseResult(
        val amount: Long,                    // 单位：分，避免浮点精度问题
        val categoryId: Long,
        val type: Int,                       // 0=支出，1=收入
        val isReimbursable: Boolean,
        val reimbursementTarget: String?,
        val note: String,
        val confidence: Int                  // 0-100，低于阈值标红提醒
    )

    operator fun invoke(input: String): ParseResult {
        // 核心解析逻辑
        // 纯 Kotlin，零 Android 依赖
        // 可以独立写单元测试
    }
}
```

**三个调用入口统一调用同一个 UseCase：**

```
APP 内输入框 ──┐
悬浮球输入框 ──┼──→  SmartParseUseCase  ──→  RecordRepository
通知监听器   ──┘
```

---

### 4.3 宠物系统解耦方案

使用 Kotlin `SharedFlow` 实现轻量级领域事件总线：

```kotlin
// domain/event/DomainEvents.kt
object DomainEvents {
    private val _events = MutableSharedFlow<DomainEvent>(
        extraBufferCapacity = 64,
        onBufferOverflow = BufferOverflow.DROP_OLDEST
    )
    val events: SharedFlow<DomainEvent> = _events.asSharedFlow()

    suspend fun emit(event: DomainEvent) = _events.emit(event)

    sealed class DomainEvent {
        data class RecordAdded(
            val record: Record,
            val isAutoDetected: Boolean
        ) : DomainEvent()

        data class ReimbursementReceived(val amount: Long) : DomainEvent()
        data class StreakBroken(val daysMissed: Int) : DomainEvent()
    }
}
```

**RecordViewModel 只管记账，不知道宠物的存在：**

```kotlin
// RecordViewModel.kt
fun addRecord(input: String) {
    val result = smartParseUseCase(input)
    val record = recordRepository.insert(result.toRecord())
    // 发布事件，宠物模块自己去监听
    viewModelScope.launch {
        DomainEvents.emit(DomainEvent.RecordAdded(record, isAutoDetected = false))
    }
}
```

**PetViewModel 自己订阅，完全独立：**

```kotlin
// PetViewModel.kt
class PetViewModel @Inject constructor(
    private val petBehaviorUseCase: PetBehaviorUseCase
) : ViewModel() {
    init {
        DomainEvents.events
            .filterIsInstance<DomainEvent.RecordAdded>()
            .onEach { event -> petBehaviorUseCase.onRecordAdded(event.record) }
            .launchIn(viewModelScope)
    }
}
```

---

### 4.4 悬浮球 Service 架构规范

`BubbleService` 是 Android Foreground Service，其生命周期与 Activity 完全独立，**不应持有 ViewModel 引用**。

```kotlin
// 正确做法：Service 直接注入 UseCase 和 Repository
@AndroidEntryPoint
class BubbleService : Service() {

    @Inject lateinit var smartParseUseCase: SmartParseUseCase
    @Inject lateinit var recordRepository: RecordRepository

    // 不持有任何 ViewModel
    // 记账完成后通过 DomainEvents 通知其他模块
}
```

---

### 4.5 宠物系统数据模型设计

```kotlin
@Entity(tableName = "pet_state")
data class PetStateEntity(
    @PrimaryKey val id: Long = 1,           // 单例行，全局只有一条
    val stage: String,                       // INFANT / JUVENILE / ADULT / ADVANCED
    val totalRecords: Int = 0,
    val lastRecordTimestamp: Long = 0,       // Unix 时间戳（毫秒）
    val currentMood: String = "NORMAL",      // HAPPY / NORMAL / SLEEPY / SAD
    val unlockedCostumeIds: String = "[]"    // JSON 数组
)
```

**情绪判断逻辑（在 PetBehaviorUseCase 中，非实时，每次打开 APP 触发一次）：**

```
今天已记账             → HAPPY
昨天记账了，今天还没   → NORMAL
连续 2 天未记账        → SLEEPY
连续 3 天及以上未记账  → SAD
```

**成长阶段判断：**

| 阶段 | 记账笔数区间 | 形态描述 |
|------|------------|---------|
| INFANT（幼年） | 0 – 30 笔 | 小小的、圆圆的 |
| JUVENILE（少年） | 31 – 100 笔 | 稍大、有小翅膀 |
| ADULT（成年） | 101 – 300 笔 | 完全体、有装饰 |
| ADVANCED（进阶） | 300 笔以上 | 特殊形态、隐藏装扮 |

---

## 五、核心架构决策（ADR）

---

### ADR-001：金额字段使用 Long 存储，单位：分

**状态：** 建议立即执行（v1.2.x 热修或 v1.3.0 前完成）

#### 背景

当前 `Record.amount: Double` 存在浮点精度问题。Java/Kotlin 的 `Double` 遵循 IEEE 754 标准，无法精确表示所有十进制小数。在金融场景中，这会导致：

- 金额显示：¥38.10 可能显示为 ¥38.099999...
- 统计汇总：多笔账目累加时误差会逐渐累积
- CSV 导出：导出数据与实际存储不一致

#### 决策

将 `Record.amount` 字段从 `Double` 改为 `Long`，存储单位为**分**（人民币最小单位）。

- 存储：38.10 元 → 存储为 `3810L`
- 显示：`3810L / 100.0` → 格式化为 `"38.10"`

提供统一的扩展函数处理转换：

```kotlin
// 分 → 显示字符串
fun Long.toAmountString(): String = "¥%.2f".format(this / 100.0)

// 用户输入字符串 → 分
fun String.toAmountCents(): Long = (this.toDouble() * 100).toLong()
```

需要编写 Room 数据库迁移：

```kotlin
val MIGRATION_1_2 = object : Migration(1, 2) {
    override fun migrate(database: SupportSQLiteDatabase) {
        // 新增临时列，转换数据，删除旧列
        database.execSQL("ALTER TABLE records ADD COLUMN amount_cents INTEGER NOT NULL DEFAULT 0")
        database.execSQL("UPDATE records SET amount_cents = CAST(amount * 100 AS INTEGER)")
        database.execSQL("ALTER TABLE records DROP COLUMN amount")
    }
}
```

#### 后果

**变得更容易：**
- 金额计算精度完全正确
- 整数运算比浮点运算更快
- 统计汇总结果完全可预测

**变得更难：**
- 需要编写 Room Migration 并测试
- 所有显示层代码需要统一使用转换函数
- 用户如果有旧版本数据，迁移期间需要处理兼容

---

### ADR-002：引入 UseCase 层封装核心业务逻辑

**状态：** 建议在 v1.3.0 开发前完成

#### 背景

智能文本识别是 JYX 最核心的差异化功能，但其解析逻辑目前以工具类形式（`VoiceCategorizer.kt`）存在，存在以下问题：

1. **逻辑分散**：悬浮球、通知监听、APP 内输入三个入口各自处理，v1.2.1 才完成统一
2. **无法单元测试**：工具类可能依赖 Android Context，无法在 JVM 环境下跑测试
3. **职责不清晰**：ViewModel 同时承担 UI 状态管理和业务逻辑

#### 决策

新建 `domain/usecase/` 目录，将以下逻辑提取为纯 Kotlin UseCase 类：

| UseCase | 职责 |
|---------|------|
| `SmartParseUseCase` | 文本解析：金额提取、分类识别、报销识别 |
| `AutoConfirmUseCase` | 自动确认规则：根据置信度阈值决策 |
| `StatisticsUseCase` | 统计聚合：月度/年度/分类汇总 |
| `PetBehaviorUseCase` | 宠物行为：根据记账行为更新宠物状态 |

**UseCase 规范：**
- 纯 Kotlin，不依赖任何 Android 框架类
- 通过构造函数注入依赖（配合 Hilt 使用）
- 单一公共操作符：`operator fun invoke(...)`
- 每个 UseCase 对应独立的单元测试文件

#### 后果

**变得更容易：**
- 智能识别逻辑可写单元测试，覆盖各种输入格式
- 三个记账入口共享同一套逻辑，彻底解决不一致问题
- ViewModel 变薄，只负责 UI 状态和事件分发

**变得更难：**
- 初期需要一次性重构现有逻辑，有时间成本
- 需要团队成员理解"ViewModel 调 UseCase，UseCase 调 Repository"的调用链

---

### ADR-003：宠物系统通过领域事件与记账解耦

**状态：** 建议在 v1.5.0 开发启动前确定

#### 背景

宠物系统的核心驱动力来自用户的记账行为（记了几笔、多久没记账）。最直觉的做法是在 `RecordViewModel` 里直接更新宠物状态，但这会引入不可接受的耦合：

- 记账功能测试时必须 Mock 宠物相关依赖
- 未来移除或重做宠物系统需要修改记账代码
- 其他功能（如成就系统）也想感知记账行为时，`RecordViewModel` 会持续膨胀

#### 决策

采用 Kotlin `SharedFlow` 实现轻量级**领域事件总线**（Domain Event Bus）。

- 记账完成 → 发布 `DomainEvent.RecordAdded`
- 宠物 ViewModel 订阅此事件 → 自行更新状态
- 记账模块对宠物模块零依赖

事件类型清单（v1.5.0 初期）：

```kotlin
sealed class DomainEvent {
    data class RecordAdded(val record: Record, val isAutoDetected: Boolean) : DomainEvent()
    data class ReimbursementReceived(val amount: Long) : DomainEvent()
    data class StreakBroken(val daysMissed: Int) : DomainEvent()
}
```

#### 后果

**变得更容易：**
- 记账模块和宠物模块可以独立开发、独立测试
- 未来新增成就系统、数据分析等功能，只需订阅相关事件，不需要修改记账代码
- 关闭宠物功能不影响记账功能（可做成开关）

**变得更难：**
- 调试时事件流不如直接调用直观，需要理解发布-订阅模式
- 需要注意 `SharedFlow` 的 `extraBufferCapacity` 和生命周期，避免事件丢失
- 事件类型增多后需要维护事件定义文档

---

### ADR-004：v2.0.0 数据库加密方案选型

**状态：** v2.0.0 开发前决策

#### 背景

v2.0.0 规划中包含数据库加密功能，需提前确定方案，因为不同方案的代码侵入程度差异较大。

#### 两个候选方案对比

| 维度 | 方案 A：SQLCipher | 方案 B：Android Keystore + 字段加密 |
|------|-----------------|--------------------------------------|
| 加密范围 | 整个数据库文件 | 仅加密 amount、note 等敏感字段 |
| APK 体积影响 | +4 MB | 极小（使用系统 API） |
| 查询性能 | 每次查询有约 5-15% 开销 | 仅解密字段时有开销 |
| 实现复杂度 | 低（替换 Room 底层驱动即可） | 高（每个敏感字段需单独处理） |
| 迁移难度 | 中（现有数据需重写为加密格式） | 高（字段级迁移更复杂） |
| 安全强度 | 高（AES-256，行业标准） | 中（取决于实现质量） |

#### 建议决策

**选择方案 A（SQLCipher）**，理由：
1. 记账 APP 用户对数据安全敏感度高，整库加密更有说服力
2. 实现代码改动极小（主要是 build.gradle 和 Room 初始化）
3. 4MB 体积增加在现代手机上完全可接受
4. SQLCipher 是金融应用加密的行业标准方案

#### 后果

**变得更容易：**
- 对用户承诺"数据库加密"时，技术实现有充分保障
- 实现代码改动小，风险可控

**变得更难：**
- 首次安装加密需要对现有用户数据做一次性迁移（需完善的迁移逻辑和回滚机制）
- APK 体积增加 4MB，需要在发版说明中说明原因

---

## 六、版本路线图重新规划

### v1.2.x（当前）→ 立即修复

| 任务 | 类型 | 说明 |
|------|------|------|
| 删除无用的 `INTERNET` 权限 | Bug Fix | 5 分钟改动，减少应用市场风险 |
| `amount: Double` → `Long` 迁移 | 数据修复 | 最好在用户量少时完成，越晚越难 |

### v1.3.0 - 体验优化（原计划 + 补充）

| 任务 | 状态 | 建议补充 |
|------|------|----------|
| #1 版本号管理规范 | 待开始 | — |
| #4 快速记账模式开关 | 待开始 | 开关状态存 DataStore，不要硬编码 |
| #5 截屏保护 | 待开始 | `FLAG_SECURE` 一行代码 |
| **提取 SmartParseUseCase** | **新增** | **重构，不加功能，但解决根本性架构问题** |
| #6 月度趋势图 | 待开始 | 聚合计算逻辑放 `StatisticsUseCase`，不要在 ViewModel 写 SQL |
| #7 左滑删除记录 | 待开始 | **建议支持撤销**（Snackbar + 延迟真正删除），体验关键点 |
| #8 识别日志脱敏 | 待开始 | — |
| #9 自动确认规则 | 待开始 | 置信度阈值存 DataStore，用户可调 |

### v1.4.0 - 数据价值挖掘

| 任务 | 状态 | 建议补充 |
|------|------|----------|
| #10 年度总览 | 待开始 | — |
| #13 搜索/筛选记录 | 待开始 | **使用 Room FTS（全文搜索）**，比 `LIKE '%keyword%'` 快 10 倍 |
| #14 兼容其他记账 APP 导入 | 待开始 | 反腐层模式：导入数据经 `ImportMapper` 转换，不直接写入核心表 |

### v1.5.0 - 悬浮球优化 + 桌面宠物基础版

| 任务 | 状态 | 建议补充 |
|------|------|----------|
| #16 桌面宠物基础版 | 待开始 | **开发前先定义 `DomainEvents`**，确保解耦架构到位 |
| BubbleService 架构规范 | **新增** | Service 直接注入 UseCase，不持有 ViewModel |

### v1.6.0 - 宠物情绪 + 成长系统

| 任务 | 状态 | 建议补充 |
|------|------|----------|
| #17 宠物情绪系统 | 待开始 | 情绪判断逻辑在 `PetBehaviorUseCase`，每次打开 APP 触发一次（省电） |
| #18 宠物成长系统 | 待开始 | 成长阶段边界值存配置，方便调整 |

### v2.0.0 - 安全大版本

| 任务 | 状态 | 建议补充 |
|------|------|----------|
| #11 应用锁 | 待开始 | 指纹/面容优先，PIN 作降级方案 |
| #12 数据库加密 | 待开始 | **推荐 SQLCipher**，参见 ADR-004 |

---

## 七、目录结构优化方案

### 目标结构

```
com.jiyixia.app/
│
├── domain/                          # 新增：纯 Kotlin，零 Android 依赖
│   ├── model/                       # 领域模型（区别于 Room Entity）
│   │   ├── Record.kt
│   │   ├── Category.kt
│   │   └── PetState.kt
│   ├── usecase/
│   │   ├── SmartParseUseCase.kt     # 智能文本解析
│   │   ├── AutoConfirmUseCase.kt    # 自动确认规则
│   │   ├── StatisticsUseCase.kt     # 统计聚合计算
│   │   └── PetBehaviorUseCase.kt    # 宠物行为逻辑
│   └── event/
│       └── DomainEvents.kt          # 领域事件总线
│
├── data/
│   ├── entity/
│   │   ├── RecordEntity.kt          # amount: Long（分）
│   │   ├── CategoryEntity.kt
│   │   └── PetStateEntity.kt        # 新增
│   ├── dao/
│   │   ├── RecordDao.kt
│   │   ├── CategoryDao.kt
│   │   └── PetStateDao.kt           # 新增
│   ├── migration/                   # 新增：DB 版本迁移文件
│   │   └── Migration_1_2.kt         # Double → Long 迁移
│   └── AppDatabase.kt
│
├── repository/
│   ├── RecordRepository.kt
│   ├── CategoryRepository.kt
│   ├── PetRepository.kt             # 新增
│   └── BackupRepository.kt
│
├── viewmodel/
│   ├── RecordViewModel.kt
│   ├── StatisticsViewModel.kt
│   ├── ReimbursementViewModel.kt
│   └── PetViewModel.kt              # 订阅 DomainEvents，不被记账模块依赖
│
├── ui/
│   ├── screens/
│   └── theme/
│
├── service/
│   ├── BubbleService.kt             # 注入 UseCase，不持有 ViewModel
│   └── PaymentNotificationListener.kt
│
└── util/
    ├── AmountFormatter.kt           # 统一的金额转换工具
    └── BackupUtil.kt
```

### 关键变化说明

| 变化 | 原因 |
|------|------|
| 新增 `domain/` 目录 | 将核心业务逻辑从 ViewModel 和工具类中提取，使其可测试 |
| 新增 `domain/event/DomainEvents.kt` | 宠物系统解耦的基础设施 |
| 新增 `data/migration/` | 所有 Room 迁移脚本集中管理，便于 Code Review |
| 新增 `util/AmountFormatter.kt` | 统一金额显示逻辑，避免各处散落 `/ 100.0` |
| `BubbleService` 不持有 ViewModel | 遵循 Android 架构规范，避免内存泄漏 |

---

## 八、优先行动清单

以下是按**投入产出比**排序的行动清单，建议按此顺序执行：

### 立即执行（< 1天工作量）

- [ ] **删除 `INTERNET` 权限**（`AndroidManifest.xml` 一行）
- [ ] **建立 `domain/usecase/` 目录结构**（空目录 + 包声明，为后续重构做准备）

### 本周内完成（重构，不加功能）

- [ ] **提取 `SmartParseUseCase`**：将 `VoiceCategorizer.kt` 和各入口的解析逻辑统一迁移进来，三个入口改为调用此 UseCase
- [ ] **为 `SmartParseUseCase` 编写单元测试**：覆盖文档中列举的 5 种输入格式，以及边界情况（纯数字、中文金额等）

### v1.3.0 开发前完成（需谨慎，有数据迁移）

- [ ] **`amount: Double` → `Long` 迁移**：编写 Room Migration，完整测试迁移流程（包括升级和降级路径）

### v1.5.0 开发前完成（宠物系统架构准备）

- [ ] **定义 `DomainEvents`**：确定初期事件类型，在 `RecordViewModel` 中添加事件发布点
- [ ] **规范 `BubbleService`**：改为直接注入 UseCase，移除对 ViewModel 的任何依赖

---

## 九、通知监听 Fix 专案（小米 HyperOS / MIUI）

> **适用场景：** 支付通知监听不起作用  
> **测试设备：** 小米 15（HyperOS 2）  
> **问题性质：** 系统策略导致（非代码 Bug），需权限引导 + 进程保活

---

### 9.1 问题根源分析

`NotificationListenerService` 在小米 HyperOS 上失效，通常是以下原因叠加：

```
原因 1：电池优化 → Service 被系统杀死
原因 2：HyperOS 通知权限需要二次授权（不同于标准 Android）
原因 3：自启动被禁止 → Service 杀死后无法重启
原因 4：应用锁 / 省电策略 → 后台任务受限
```

**关键认知：不是代码 Bug，是系统策略。** 小米、OPPO、vivo 等国产 ROM 对后台进程管控远超原生 Android，需要代码 + 用户引导双管齐下。

---

### 9.2 第一层：确认通知监听权限是否真正授予

小米有**两层**通知权限，仅系统弹窗点"允许"不够：

```kotlin
// util/PermissionChecker.kt
object PermissionChecker {

    /**
     * 检查 NotificationListenerService 是否真正被系统启用
     * 注意：这个方法在小米上可能返回 true，但实际监听仍被截断，
     * 需要配合下面的小米专属权限检查
     */
    fun isNotificationListenerEnabled(context: Context): Boolean {
        val enabledListeners = Settings.Secure.getString(
            context.contentResolver,
            "enabled_notification_listeners"
        )
        return enabledListeners?.contains(context.packageName) == true
    }

    /**
     * 跳转到系统通知使用权设置页（通用方案）
     */
    fun requestNotificationListenerPermission(context: Context) {
        val intent = Intent(Settings.ACTION_NOTIFICATION_LISTENER_SETTINGS)
        context.startActivity(intent)
    }

    /**
     * 跳转到小米专属"通知使用权"设置页
     * 这是小米二次授权的关键入口
     */
    fun requestMiuiNotificationPermission(context: Context) {
        try {
            val intent = Intent("miui.intent.action.APP_PERM_EDITOR")
            intent.setClassName(
                "com.miui.securitycenter",
                "com.miui.permcenter.permissions.PermissionsEditorActivity"
            )
            intent.putExtra("extra_pkgname", context.packageName)
            context.startActivity(intent)
        } catch (e: Exception) {
            // 降级到标准设置页
            context.startActivity(Intent(Settings.ACTION_NOTIFICATION_LISTENER_SETTINGS))
        }
    }
}
```

---

### 9.3 第二层：重写 `PaymentNotificationListener` — 加自我守护逻辑

```kotlin
// service/PaymentNotificationListener.kt
class PaymentNotificationListener : NotificationListenerService() {

    override fun onListenerConnected() {
        super.onListenerConnected()
        Log.d(TAG, "NotificationListener connected at ${System.currentTimeMillis()}")
        // 可在此处持久化连接状态
    }

    /**
     * HyperOS 断开连接时的自动重连
     * 这是最关键的一步：系统断开时请求重新绑定
     */
    override fun onListenerDisconnected() {
        super.onListenerDisconnected()
        Log.w(TAG, "NotificationListener disconnected at ${System.currentTimeMillis()}, requesting rebind...")
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.N) {
            requestRebind(
                ComponentName(this, PaymentNotificationListener::class.java)
            )
        }
    }

    /**
     * 通知到达回调 — 只过滤支付相关通知
     */
    override fun onNotificationPosted(sbn: StatusBarNotification?) {
        sbn ?: return

        // 只处理微信、支付宝的通知
        if (sbn.packageName !in PAYMENT_PACKAGES) return

        val notification = sbn.notification ?: return
        val extras = notification.extras ?: return

        val title = extras.getString(Notification.EXTRA_TITLE) ?: ""
        val text = extras.getCharSequence(Notification.EXTRA_TEXT)?.toString() ?: ""

        handlePaymentNotification(title, text, sbn.packageName)
    }

    /**
     * 处理支付通知 — 过滤 + 转发
     * 注意：不在 Service 里写解析逻辑，通过 BroadCast 或 Repository 转发
     */
    private fun handlePaymentNotification(
        title: String,
        text: String,
        packageName: String
    ) {
        val combined = "$title $text"
        if (!isPaymentNotification(combined)) return

        val intent = Intent(ACTION_PAYMENT_DETECTED).apply {
            putExtra(EXTRA_TEXT, combined)
            putExtra(EXTRA_SOURCE, packageName)
        }
        LocalBroadcastManager.getInstance(this).sendBroadcast(intent)
    }

    /**
     * 判断通知内容是否包含支付关键字
     */
    private fun isPaymentNotification(text: String): Boolean {
        val keywords = listOf(
            "付款成功", "支付成功", "收款", "转账", "消费",
            "支出", "余额变动", "交易提醒", "扣款"
        )
        return keywords.any { text.contains(it) }
    }

    /**
     * 处理被移除的通知（部分 ROM 会在支付后立即移除通知）
     */
    override fun onNotificationRemoved(sbn: StatusBarNotification?) {
        super.onNotificationRemoved(sbn)
        // HyperOS 可能先 post 再 remove，这里不做额外处理
    }

    companion object {
        const val ACTION_PAYMENT_DETECTED = "com.jiyixia.PAYMENT_DETECTED"
        const val EXTRA_TEXT = "extra_text"
        const val EXTRA_SOURCE = "extra_source"

        // 支付相关 APP 的包名
        val PAYMENT_PACKAGES = setOf(
            "com.tencent.mm",                     // 微信
            "com.eg.android.AlipayGphone",        // 支付宝
            "com.unionpay",                        // 云闪付（可选）
            "com.tencent.mobileqq"                 // QQ钱包（可选）
        )

        private const val TAG = "PaymentListener"
    }
}
```

---

### 9.4 第三层：Foreground Service 保活

HyperOS 对后台 Service 管控极其严格。**必须**用 Foreground Service 包裹或辅助保活：

```kotlin
// service/NotificationKeepAliveService.kt
class NotificationKeepAliveService : Service() {

    override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
        createNotificationChannel()
        startForegroundNotification()

        // START_STICKY：被杀死后系统自动重启（在小米上不保证，但聊胜于无）
        return START_STICKY
    }

    override fun onBind(intent: Intent?): IBinder? = null

    private fun startForegroundNotification() {
        val notification = NotificationCompat.Builder(this, CHANNEL_ID)
            .setContentTitle("记一下")
            .setContentText("自动记账监听运行中")
            .setSmallIcon(R.drawable.ic_notification)
            .setPriority(NotificationCompat.PRIORITY_MIN)
            .setOngoing(true)
            .setSilent(true)                     // 静默，不发出通知声音
            .build()

        startForeground(NOTIFICATION_ID, notification)
    }

    private fun createNotificationChannel() {
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
            val channel = NotificationChannel(
                CHANNEL_ID,
                "自动记账",
                NotificationManager.IMPORTANCE_MIN  // 最低重要性，不打扰用户
            ).apply {
                setShowBadge(false)
                enableLights(false)
                enableVibration(false)
                setSound(null, null)
            }
            getSystemService(NotificationManager::class.java)
                ?.createNotificationChannel(channel)
        }
    }

    override fun onDestroy() {
        super.onDestroy()
        // Service 销毁时记录日志，便于排查
        Log.w(TAG, "KeepAliveService destroyed")
    }

    companion object {
        const val CHANNEL_ID = "keep_alive_channel"
        const val NOTIFICATION_ID = 1001
        private const val TAG = "KeepAlive"
    }
}
```

---

### 9.5 第四层：HyperOS 专属权限引导工具

```kotlin
// util/MiuiPermissionHelper.kt
object MiuiPermissionHelper {

    /**
     * 判断当前是否运行在 MIUI / HyperOS 上
     */
    fun isMiui(): Boolean {
        return getSystemProperty("ro.miui.ui.version.name").isNotEmpty()
    }

    fun isHyperOs(): Boolean {
        return getSystemProperty("ro.mi.os.version.name").isNotEmpty()
    }

    /**
     * 跳转到小米"自启动管理"页面
     * 路径：设置 → 应用设置 → 应用管理 → 记一下 → 自启动
     *
     * 这是国产机最常见的坑：自启动不开，后台 Service 永远活不了
     */
    fun openAutoStartSettings(context: Context) {
        val intents = listOf(
            // HyperOS / MIUI 14+
            Intent().apply {
                component = ComponentName(
                    "com.miui.securitycenter",
                    "com.miui.permcenter.autostart.AutoStartManagementActivity"
                )
            },
            // 旧版 MIUI 降级方案
            Intent().apply {
                action = "miui.intent.action.OP_AUTO_START"
                addCategory(Intent.CATEGORY_DEFAULT)
            }
        )

        for (intent in intents) {
            try {
                context.startActivity(intent)
                return
            } catch (e: ActivityNotFoundException) {
                continue
            }
        }

        // 所有小米专属入口都失败，跳转到通用应用设置
        val fallback = Intent(Settings.ACTION_APPLICATION_DETAILS_SETTINGS).apply {
            data = Uri.fromParts("package", context.packageName, null)
        }
        context.startActivity(fallback)
    }

    /**
     * 跳转到"电池优化"设置页
     * 关闭电池优化后，系统不会因为省电原因杀死 Service
     */
    fun openBatteryOptimizationSettings(context: Context) {
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
            try {
                val intent = Intent(Settings.ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS)
                context.startActivity(intent)
            } catch (e: Exception) {
                context.startActivity(
                    Intent(Settings.ACTION_APPLICATION_DETAILS_SETTINGS).apply {
                        data = Uri.fromParts("package", context.packageName, null)
                    }
                )
            }
        }
    }

    /**
     * 反射读取系统属性
     */
    private fun getSystemProperty(key: String): String {
        return try {
            val clazz = Class.forName("android.os.SystemProperties")
            clazz.getMethod("get", String::class.java)
                .invoke(null, key) as? String ?: ""
        } catch (e: Exception) {
            ""
        }
    }
}
```

---

### 9.6 第五层：权限引导 UI（首次启动弹窗）

```kotlin
// ui/screens/PermissionGuideScreen.kt
@Composable
fun NotificationPermissionGuideScreen(
    onComplete: () -> Unit
) {
    val context = LocalContext.current

    Scaffold { paddingValues ->
        Column(
            modifier = Modifier
                .fillMaxSize()
                .padding(paddingValues)
                .padding(24.dp),
            verticalArrangement = Arrangement.Center
        ) {
            // 标题区
            Text(
                text = "开启自动记账",
                style = MaterialTheme.typography.headlineMedium,
                fontWeight = FontWeight.Bold
            )

            Spacer(modifier = Modifier.height(8.dp))

            Text(
                text = "需要开启以下 3 项权限，才能自动识别支付通知",
                style = MaterialTheme.typography.bodyLarge,
                color = MaterialTheme.colorScheme.onSurfaceVariant
            )

            Spacer(modifier = Modifier.height(32.dp))

            // 权限步骤列表
            PermissionStepCard(
                stepNumber = 1,
                title = "通知使用权",
                description = "允许「记一下」读取支付通知，用于自动记账",
                icon = Icons.Default.Notifications,
                onClick = {
                    context.startActivity(
                        Intent(Settings.ACTION_NOTIFICATION_LISTENER_SETTINGS)
                    )
                }
            )

            Spacer(modifier = Modifier.height(12.dp))

            PermissionStepCard(
                stepNumber = 2,
                title = "开启自启动",
                description = "防止系统杀死监听服务（小米/HyperOS 必须手动开启）",
                icon = Icons.Default.PlayArrow,
                onClick = {
                    MiuiPermissionHelper.openAutoStartSettings(context)
                }
            )

            Spacer(modifier = Modifier.height(12.dp))

            PermissionStepCard(
                stepNumber = 3,
                title = "关闭电池优化",
                description = "确保后台监听稳定运行，不被省电策略中断",
                icon = Icons.Default.BatterySaver,
                onClick = {
                    MiuiPermissionHelper.openBatteryOptimizationSettings(context)
                }
            )

            Spacer(modifier = Modifier.height(32.dp))

            // 确认按钮
            Button(
                onClick = onComplete,
                modifier = Modifier.fillMaxWidth(),
                colors = ButtonDefaults.buttonColors(
                    containerColor = MaterialTheme.colorScheme.primary
                )
            ) {
                Text("已完成设置", modifier = Modifier.padding(vertical = 4.dp))
            }

            Spacer(modifier = Modifier.height(12.dp))

            Text(
                text = "提示：如果仍无法监听，请检查系统「设置 → 应用管理 → 记一下 → 权限」中是否全部开启",
                style = MaterialTheme.typography.bodySmall,
                color = MaterialTheme.colorScheme.onSurfaceVariant,
                textAlign = TextAlign.Center,
                modifier = Modifier.fillMaxWidth()
            )
        }
    }
}

/**
 * 权限步骤卡片组件
 */
@Composable
private fun PermissionStepCard(
    stepNumber: Int,
    title: String,
    description: String,
    icon: ImageVector,
    onClick: () -> Unit
) {
    Card(
        modifier = Modifier
            .fillMaxWidth()
            .clickable(onClick = onClick),
        colors = CardDefaults.cardColors(
            containerColor = MaterialTheme.colorScheme.surfaceVariant.copy(alpha = 0.5f)
        ),
        shape = RoundedCornerShape(12.dp)
    ) {
        Row(
            modifier = Modifier.padding(16.dp),
            verticalAlignment = Alignment.CenterVertically
        ) {
            // 步骤编号
            Surface(
                shape = CircleShape,
                color = MaterialTheme.colorScheme.primary,
                modifier = Modifier.size(28.dp)
            ) {
                Box(contentAlignment = Alignment.Center) {
                    Text(
                        text = "$stepNumber",
                        color = MaterialTheme.colorScheme.onPrimary,
                        fontWeight = FontWeight.Bold,
                        fontSize = 14.sp
                    )
                }
            }

            Spacer(modifier = Modifier.width(12.dp))

            Icon(
                imageVector = icon,
                contentDescription = null,
                tint = MaterialTheme.colorScheme.primary,
                modifier = Modifier.size(24.dp)
            )

            Spacer(modifier = Modifier.width(12.dp))

            Column(modifier = Modifier.weight(1f)) {
                Text(
                    text = title,
                    style = MaterialTheme.typography.titleSmall,
                    fontWeight = FontWeight.SemiBold
                )
                Text(
                    text = description,
                    style = MaterialTheme.typography.bodySmall,
                    color = MaterialTheme.colorScheme.onSurfaceVariant
                )
            }

            Icon(
                imageVector = Icons.Default.ChevronRight,
                contentDescription = "去设置",
                tint = MaterialTheme.colorScheme.onSurfaceVariant
            )
        }
    }
}
```

---

### 9.7 AndroidManifest.xml 配置检查

确认当前 `AndroidManifest.xml` 中以下配置完整：

```xml
<!-- 通知监听 Service — 必须 exported=true 且有 BIND_NOTIFICATION_LISTENER_SERVICE 权限 -->
<service
    android:name=".service.PaymentNotificationListener"
    android:label="自动记账监听"
    android:exported="true"
    android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
    <intent-filter>
        <action android:name="android.service.notification.NotificationListenerService" />
    </intent-filter>
</service>

<!-- Keep-Alive Foreground Service — 用于进程保活 -->
<service
    android:name=".service.NotificationKeepAliveService"
    android:foregroundServiceType="dataSync"
    android:exported="false" />

<!-- 权限声明 -->
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
```

---

### 9.8 完整排查流程

建议在代码中加日志，按以下顺序排查：

```
第 1 步：adb logcat | grep -i "PaymentNotification"
         → 确认 Service 是否被系统创建

第 2 步：调用 isNotificationListenerEnabled()
         → 确认通知监听权限是否被系统真正启用

第 3 步：引导用户去小米安全中心开启「自启动」
         → openAutoStartSettings()

第 4 步：引导用户关闭电池优化
         → openBatteryOptimizationSettings()

第 5 步：检查 onListenerDisconnected 日志
         → 确认 requestRebind() 是否被调用

第 6 步：启动 NotificationKeepAliveService（Foreground）
         → START_STICKY + foreground notification 兜底

第 7 步：如果以上全做了仍无效
         → 引导用户去「设置 → 应用管理 → 记一下 → 权限」手动检查全部权限
         → 部分 HyperOS 版本存在隐藏的「后台限制」开关
```

---

### 9.9 其他国产 ROM 适配参考

| 品牌 | 系统 | 特殊处理 |
|------|------|----------|
| 小米 | HyperOS / MIUI | 自启动 + 电池优化 + 通知使用权三重权限 |
| 华为 | HarmonyOS / EMUI | 启动管理 → 允许自启动 + 关联启动 + 后台活动 |
| OPPO | ColorOS | 电池 → 应用速冻 → 关闭速冻 |
| vivo | OriginOS | 后台高耗电 → 允许后台运行 |
| 三星 | One UI | 优化电池使用量 → 不受限制 |

---

## 附录：快速参考

### 金额存储转换规则

```kotlin
// 存储（用户输入 → 数据库）
"38.10".toAmountCents()  // → 3810L

// 显示（数据库 → UI）
3810L.toAmountString()   // → "¥38.10"

// 统计（避免浮点累加）
records.sumOf { it.amount }  // Long 整数相加，精确无误
```

### Room FTS 搜索（v1.4.0 参考）

```kotlin
// 比 LIKE '%keyword%' 快 10 倍
@Fts4(contentEntity = RecordEntity::class)
@Entity(tableName = "records_fts")
data class RecordFtsEntity(
    val note: String
)

// DAO
@Query("SELECT * FROM records WHERE rowid IN (SELECT rowid FROM records_fts WHERE records_fts MATCH :query)")
fun searchRecords(query: String): Flow<List<RecordEntity>>
```

### 宠物情绪判断（v1.5.0 参考）

```kotlin
fun determineMood(lastRecordTimestamp: Long, now: Long = System.currentTimeMillis()): PetMood {
    val daysSinceLastRecord = TimeUnit.MILLISECONDS.toDays(now - lastRecordTimestamp)
    return when {
        daysSinceLastRecord == 0L -> PetMood.HAPPY    // 今天已记账
        daysSinceLastRecord == 1L -> PetMood.NORMAL   // 昨天记账了
        daysSinceLastRecord == 2L -> PetMood.SLEEPY   // 2 天未记账
        else -> PetMood.SAD                            // 3 天及以上
    }
}
```

---

*本报告由架构分析生成，基于 APP 说明文档 v1.2.1。如有疑问或需进一步讨论，请联系文档作者。*

*报告生成时间：2026-06-09（v1.1 更新：新增通知监听 Fix 专案）*

# 记一下 - 待办任务清单

> 按实现难度排序，每完成一项更新状态并提交版本。

---

## 🟢 简单

### #1 版本号管理规范
- **状态**：待开始
- **描述**：建立版本管理流程，每次功能/修复完成同步升级版本号
- **改动范围**：流程约定（build.gradle.kts + CHANGELOG.md）
- **验收标准**：每次提交前检查 versionCode/versionName 是否已更新

### #2 CSV 导出路径整改
- **状态**：暂缓（合并到 #12 一起改）
- **描述**：CSV 当前写到公共 Downloads/ 目录，改为写入 App 内部目录 files/JiYiXia/export/
- **改动范围**：SettingsScreen.kt 的 exportCsv() 函数
- **验收标准**：导出文件在 App 目录内，用户通过「分享」功能获取文件

### #3 数据库备份路径整改
- **状态**：暂缓（合并到 #12 一起改）
- **描述**：BackupUtil 备份路径改为 App 内部目录 files/JiYiXia/backup/
- **改动范围**：BackupUtil.kt
- **验收标准**：备份/恢复路径均在 App 目录内

### #4 快速记账模式开关
- **状态**：待开始
- **描述**：设置页新增「记账模式」开关，支持极速模式（500ms 自动保存）和确认模式（手动点按钮保存）
- **改动范围**：SettingsScreen.kt + QuickRecordScreen.kt + DataStore
- **验收标准**：两种模式可切换，偏好持久化

### #5 截屏保护
- **状态**：待开始
- **描述**：FLAG_SECURE 防止截屏录屏泄露财务数据，设置页开关控制
- **改动范围**：MainActivity.kt + SettingsScreen.kt
- **验收标准**：开关控制截屏保护，关闭后允许截屏

---

## 🟡 中等

### #6 月度趋势图
- **状态**：待开始
- **描述**：StatsScreen 的「月度趋势」Tab 当前是占位文案，用 Canvas 绘制近 6 个月收支柱状图
- **改动范围**：StatsScreen.kt + StatsViewModel.kt
- **验收标准**：展示近 6 个月的收支柱状图，支持月份标签和数值

### #7 左滑删除记录
- **状态**：待开始
- **描述**：首页记录列表用 SwipeToDismiss 实现左滑删除，右侧按钮精简为只留编辑
- **改动范围**：HomeScreen.kt
- **验收标准**：左滑出现删除操作，交互流畅

### #8 识别日志脱敏
- **状态**：待开始
- **描述**：设置页识别日志只显示时间+分类+金额，不暴露原始通知全文
- **改动范围**：SettingsScreen.kt + PaymentNotificationListener.kt
- **验收标准**：日志不包含完整通知文本

### #9 自动确认规则
- **状态**：待开始
- **描述**：设置页「自动确认规则」功能实现，置信度阈值可调（默认 80%）
- **改动范围**：SettingsScreen.kt + DataStore + PaymentNotificationListener.kt
- **验收标准**：阈值可配置，超过阈值的记录自动确认

### #10 年度总览
- **状态**：待开始
- **描述**：新增年度汇总页：12 个月收支柱状图 + 年度结余 + Top 分类
- **改动范围**：新增 Screen + ViewModel + DAO 查询
- **验收标准**：可查看年度收支汇总和分类排行

---

## 🔴 较复杂

### #11 应用锁（指纹/面容/PIN）
- **状态**：待开始
- **描述**：启动 App 时验证身份，使用 BiometricPrompt API，设置页开关+备用 PIN
- **改动范围**：新增应用锁模块 + MainActivity + SettingsScreen
- **验收标准**：支持指纹/面容/PIN 三种方式，可开关

### #12 数据库加密 + 文件路径统一整改
- **状态**：待开始
- **描述**：引入 SQLCipher 加密数据库，密钥存 Android Keystore；同时整改 CSV 导出和备份路径
- **改动范围**：AppDatabase + 依赖引入 + BackupUtil + SettingsScreen + 数据迁移
- **验收标准**：数据库加密存储，所有文件路径在 App 目录内

### #13 搜索/筛选记录
- **状态**：待开始
- **描述**：首页新增搜索入口，支持按金额范围、分类、关键词、日期范围筛选
- **改动范围**：DAO + Repository + HomeScreen
- **验收标准**：搜索结果准确，支持多条件组合筛选

---

## 版本规划

| 版本 | 包含任务 | 备注 |
|---|---|---|
| v1.2.1 | #1 + #4 + #5 | 设置相关，集中做 |
| v1.3.0 | #6 + #7 + #8 + #9 | 体验优化 |
| v1.4.0 | #10 + #13 | 数据价值挖掘 |
| v2.0.0 | #11 + #12 | 安全大版本 |

---

## 规则

1. 每完成一个任务 → 更新本文件状态 → 更新版本号 → 提交+tag → 推送 GitHub → 构建 APK
2. APK 命名格式：`记一下_v{版本号}.apk`
3. CHANGELOG 同步更新
